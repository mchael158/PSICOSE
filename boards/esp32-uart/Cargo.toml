[package]
name = "esp32-uart"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
publish = false
license = "MIT OR Apache-2.0"
description = "PSICOSE on ESP32 classic UART1 — hardware example (not published)"

[dependencies]
# psicose: zero-deps motor. HAL lives only in this board package.
psicose = { workspace = true }
esp-hal = { workspace = true }
esp-backtrace = { workspace = true }
esp-println = { workspace = true }
esp-bootloader-esp-idf = { workspace = true }
